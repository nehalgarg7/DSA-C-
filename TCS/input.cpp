#include <bits/stdc++.h>
using namespace std;

int main()
{
    // string s;
    // getline(cin , s);

    // s.erase(s.begin());
    // s.erase(s.end()-1);
    // stringstream ss(s);
    // vector<int> arr;

    // string token;
    // char delimiter = ',';

    // while(getline(ss, token, delimiter))
    // {
    //     arr.push_back(stoi(token));
    // }

    // vector<int> arr;
    // string input;
    // getline(cin, input);
    // stringstream ss(input);
    // int num;

    // while (ss >> num)
    // {
    //     arr.push_back(num);
    // }

    // for (auto ele : arr)
    // {
    //     cout << ele << " ";
    // }

    // string str;
    
    // getline(cin ,str);

    // str.erase(str.begin());
    // str.erase(str.end()-1);



    
    return 0;
}